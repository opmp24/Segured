// COPY to github-config.js and fill values. Do NOT commit tokens to public repos.
const GITHUB_CONFIG = {
  owner: "opmp24", // repo owner
  repo: "Segured",  // repo name
  branch: "main",
  galleryPath: "gallery",
  documentsPath: "documents"
};
